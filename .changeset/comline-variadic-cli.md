---
"agents.yaml": minor
---

Use Comline for typed command routing, variadic paths, generated help, and shell
completion while preserving the interactive Clack flow. Reject invalid commands
and options before execution, support explicit boolean values, report the installed
package version, and return a failing exit status for invalid JSON validation.
